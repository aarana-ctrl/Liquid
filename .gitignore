# Dependencies
node_modules/

# Build output
dist/
build/
site*/

# Vite temp files
**/vite.config.js.timestamp-*.mjs

# Env & secrets
.env
.env.*
!.env.example

# OS / editor junk
.DS_Store
*.log

# Raw wallpaper sources (multi-GB 4K originals). Only the compressed, web-ready
# copies in web-app/public/themes/ are committed + deployed. Regenerate with
# ./compress-backgrounds.sh
/Themes/
extension.zip
