# Netlify config — drop this repo in and it deploys with no extra setup.
[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback so client routes resolve.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
